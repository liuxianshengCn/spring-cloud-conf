server:
  port: 8090
spring:
  application:
      name: server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liuxianshengCn/spring-cloud-conf/
          searchPaths: config-repo
          #username:
          #password: